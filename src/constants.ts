enum APP_ENVIRONMENTS {
	development = 'development',
	production = 'production',
}

enum LOG_LEVELS {
	error = 'error',
	warn = 'warn',
	info = 'info',
	http = 'http',
	verbose = 'verbose',
	debug = 'debug',
	silly = 'silly',
}

export { APP_ENVIRONMENTS, LOG_LEVELS };
